/**
 * No Tailwind. The brutalist design system is hand-authored CSS —
 * see src/app/globals.css and src/styles/tokens.css.
 * Next.js handles plain CSS @import, nesting and autoprefixing on its own.
 */
const config = {
  plugins: {},
};

export default config;
